import 'package:flutter/material.dart';

class TarjetasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Tarjetas Screen"),
      ),
    );
  }
}